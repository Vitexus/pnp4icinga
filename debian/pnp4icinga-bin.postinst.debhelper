# Automatically added by dh_installinit
if [ -x "/etc/init.d/npcd" ]; then
	update-rc.d npcd defaults >/dev/null
	invoke-rc.d npcd start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/pnp_gearman_worker" ]; then
	update-rc.d pnp_gearman_worker defaults >/dev/null
	invoke-rc.d pnp_gearman_worker start || exit $?
fi
# End automatically added section
