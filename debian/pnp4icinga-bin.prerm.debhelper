# Automatically added by dh_installinit
if [ -x "/etc/init.d/pnp_gearman_worker" ]; then
	invoke-rc.d pnp_gearman_worker stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/npcd" ]; then
	invoke-rc.d npcd stop || exit $?
fi
# End automatically added section
